# Dead Code Report — ReqGPT / ThinkingPods

Generated by AST-based static analysis + manual verification.
Date: 2026-07-28

---

## SAFE TO REMOVE (no importers, no runtime effect)

### 1. `use_reqgpt.py`
- **Status**: Entire file is dead.
- **Why**: Legacy Mistral-7B + PEFT adapter path (~28GB RAM on CPU) from HuggingFace. Zero importers across all project `.py` files. Not wired into `server.py` or any live pipeline. (Per AGENTS.md: "use_reqgpt.py is the legacy/dead path".)
- **Recommendation**: Delete. ~220 lines removed, eliminated if the repo is ever revived for that path it's in git history.

### 2. `use_ollama.py`
- **Status**: Entire file is dead.
- **Why**: Contains `OllamaClient` class plus helper functions. Zero importers. The live Ollama integration lives inline in `server.py` (lines ~75–130 of that file). This module was written as a separate abstraction but never integrated.
- **Recommendation**: Delete.

### 3. `ResponseStrategy.SUMMARIZE` enum value — `contracts.py:148`
- **Status**: Dead enum member.
- **Why**: `SUMMARIZE = "SUMMARIZE"` is defined inside `class ResponseStrategy(str, Enum)` but **never referenced** anywhere in the codebase — no `ResponseStrategy.SUMMARIZE`, no `"SUMMARIZE"` string comparison, no mapping, no import. Its purpose was superseded by `GENERATE_SUMMARY` (which IS used extensively).
- **Recommendation**: Remove the line. Downstream `is_summary_strategy()` checks `GENERATE_SUMMARY` (and a future `CORRECT`), never `SUMMARIZE`.

### 4. Stale docstring — `contracts.py:364`
- **Status**: Misleading comment, not code.
- **Why**: `summary_sections` field says `"""If SUMMARIZE: which hypothesis kinds to include in the summary."""` but should reference `GENERATE_SUMMARY`.
- **Recommendation**: Fix to `GENERATE_SUMMARY`. Cosmetic only.

---

## ENTRY POINTS (standalone, intentionally never imported)

These files are never imported by another `.py` module because they are **entry-point scripts** launched directly via `python <file>.py` or `.bat` launchers. They import from the project but are not imported by it. Not dead by architecture — just not library code.

| File | Launched via |
|------|-------------|
| `dina_direct.py` | `run_dina_direct.bat` |
| `design_thinking_coach.py` | `run_design_thinking_coach.bat` |
| `design_thinking_coach_text.py` | `run_design_thinking_coach_text.bat` |
| `autotweak.py` | `run.bat` (first step) |
| `diagnose.py` | `python diagnose.py` (manual) |
| `run.py` | `python run.py` or `run.bat` |
| `dead_code_analyzer.py` | `python dead_code_analyzer.py` (this tool) |

**Recommendation**: Keep all. Consider moving entry points into a `bin/` or `scripts/` directory if project grows.

---

## VERIFY FIRST (potentially dead but unclear)

None found. All remaining modules have at least one live importer:

| Module | Imported by |
|--------|-------------|
| `memory.py` | `server.py`, `dina_direct.py`, `design_thinking_coach.py`, `design_thinking_coach_text.py` |
| `knowledge_base.py` | `inference_engine.py`, `hypothesis_manager.py` |
| `inference_engine.py` | `mentor.py` |
| `state_manager.py` | `mentor.py`, `tests/` |
| `session_manager.py` | `mentor.py`, `server.py` |
| `contracts.py` | 10+ files |
| `constants.py` | `app.py`, `server.py` |

---

## FALSE POSITIVES (reported by auto-scan but NOT dead)

The initial AST scan of `dead_code_analyzer.py` scanned `.venv/` and produced many false positives:

- `web_search.py`, `dd.py`, `ollama_transcribe.py`, `st_utils.py`, `st_voice.py` — **do not exist** in this project (were `.venv` packages)
- `routers/`, `openai_style/`, `utils/`, `st_pages/`, `batch/` — **do not exist** as project directories
- `MemoryManager` alias (`mentor.py:352`, `MemoryManager = SessionManager`) — **not dead**: imported by `server.py`, `dina_direct.py`, `design_thinking_coach.py`, `design_thinking_coach_text.py`
- `ChecklistManager` — **not dead**: used in `mentor.py`, `server.py`, tests, CLI tools
- `RecipeGenerator` — **does not exist** in this project (was a `.venv` artifact)

---

## Summary Table

| Item | Classification | Lines | Action |
|------|---------------|-------|--------|
| `use_reqgpt.py` | SAFE TO REMOVE | ~220 | Delete |
| `use_ollama.py` | SAFE TO REMOVE | ~200 | Delete |
| `ResponseStrategy.SUMMARIZE` | SAFE TO REMOVE | 1 line | Delete enum member |
| `contracts.py:364` docstring | SAFE TO REMOVE | 1 word | Fix to `GENERATE_SUMMARY` |
| 7 entry-point scripts | KEEP | varies | No action |
| `MemoryManager` alias | KEEP | 1 line | No action (exported) |
| `ChecklistManager` | KEEP | ~60 lines | No action (in use) |
| All other project modules | KEEP | — | Verified imported |

---

## What was NOT found

- No orphaned test files (all `tests/test_*.py` import at least one production module)
- No duplicate class/enum definitions across modules (the `ResponseStrategy` in `contracts.py` is the canonical one; `module4/response_strategy.py` imports it)
- No unreachable branches in pipeline code (beyond the one dead enum value)